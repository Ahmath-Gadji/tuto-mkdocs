[project]
name = "wolof-mkdocs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-git-committers-plugin-2>=2.5.0",
    "pip>=25.2",
]
